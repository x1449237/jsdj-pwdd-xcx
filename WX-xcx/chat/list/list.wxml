<view class="chat-list-page">
  <!-- 顶部标签页 -->
  <view class="tab-bar">
    <view
      class="tab-item {{currentTab === 'chat' ? 'tab-item-active' : ''}}"
      bindtap="onTabChange"
      data-tab="chat"
    >
      <text class="tab-text">订单私聊</text>
      <view class="tab-indicator" wx:if="{{currentTab === 'chat'}}"></view>
    </view>
    <view
      class="tab-item {{currentTab === 'group' ? 'tab-item-active' : ''}}"
      bindtap="onTabChange"
      data-tab="group"
    >
      <text class="tab-text">群聊</text>
      <view class="tab-indicator" wx:if="{{currentTab === 'group'}}"></view>
    </view>
    <view
      class="tab-item {{currentTab === 'after_sale' ? 'tab-item-active' : ''}}"
      bindtap="onTabChange"
      data-tab="after_sale"
    >
      <text class="tab-text">售后</text>
      <view class="tab-indicator" wx:if="{{currentTab === 'after_sale'}}"></view>
    </view>
  </view>

  <!-- 搜索栏 -->
  <view class="search-bar">
    <view class="search-input-wrapper">
      <text class="search-icon">🔍</text>
      <input
        class="search-input"
        placeholder="搜索聊天"
        placeholder-class="placeholder"
        value="{{searchKeyword}}"
        bindinput="onSearchInput"
        confirm-type="search"
        bindconfirm="onSearch"
      />
    </view>
  </view>

  <!-- 订单私聊列表 -->
  <view class="conversation-list" wx:if="{{currentTab === 'chat'}}">
    <block wx:if="{{conversationList.length > 0}}">
      <view
        wx:for="{{conversationList}}"
        wx:key="conversation_id"
        class="conversation-item"
        bindtap="onOpenChat"
        data-conversation="{{item}}"
        bindlongpress="onLongPress"
        data-conversation="{{item}}"
      >
        <view class="avatar-wrapper">
          <image class="avatar" src="{{item.avatar}}" mode="aspectFill"></image>
          <view class="unread-badge" wx:if="{{item.unread_count > 0}}">
            <text>{{item.unread_count > 99 ? '99+' : item.unread_count}}</text>
          </view>
        </view>

        <view class="conversation-info">
          <view class="info-header">
            <text class="nickname">{{item.nickname}}</text>
            <text class="time">{{item.last_time}}</text>
          </view>
          <view class="info-footer">
            <text class="last-message text-ellipsis">{{item.last_message}}</text>
          </view>
        </view>
      </view>
    </block>

    <view class="empty-state" wx:if="{{conversationList.length === 0 && !loading}}">
      <text class="empty-state-icon">💬</text>
      <text class="empty-state-text">暂无聊天消息</text>
    </view>

    <view class="loading-wrapper" wx:if="{{loading}}">
      <text class="loading-more">加载中...</text>
    </view>
  </view>
</view>